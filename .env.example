# Module 3 runs fully offline — the test suite uses injected in-memory mocks for
# the model client. You do NOT need any values here to pass CI.
#
# If you want to try the extractor against a real LLM, wire your own client that
# implements the ModelClient protocol (complete + get_call_count). Never commit API keys.
