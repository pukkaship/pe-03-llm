{
  "name": "pe-03-llm",
  "version": "1.0.0",
  "private": true,
  "description": "Module 3 — First LLM classifier + transport vs content. Fix five deliberate bugs, one gate at a time.",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "begin": "node scripts/begin.cjs",
    "unlock": "node scripts/unlock.cjs",
    "validate": "node scripts/validate_pr.cjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^3.2.4"
  },
  "allowScripts": {
    "esbuild": true,
    "fsevents": true
  }
}
