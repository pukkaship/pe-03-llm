# Module 3 runs fully offline by default — the test suite uses in-memory mocks for
# any external dependency. You do NOT need any values here to pass CI.
#
# TODO(scaffold): if the real module has an OPTIONAL "prove it against a real service" path,
# document the variables here (commented out). Never commit real values.
